src/webhook_config.h
.backup-excludes
.clangd
backup
build

# Real Discord webhook URL -- see src/webhook_config.example.h for the
# committed template this is copied from. Never commit the real one.
src/webhook_config.h